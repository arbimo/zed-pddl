# Minimalist Zed extension for PDDL and friends

At this point, the extension only provides syntax highlighting and outline.

Planned: provide a number of tasks in `language/pddl/tasks.json` that provide common operations (parsing, solving, opening domain, ...) based on `aries-plan-engine` or `unified-planning`.

<!--
```
cargo install --bin ape --git https://github.com/plaans/aries aries-plan-engine
```
-->
